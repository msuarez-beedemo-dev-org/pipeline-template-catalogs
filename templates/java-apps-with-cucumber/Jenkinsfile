pipeline {
  agent {
    kubernetes
    {
    label 'java-app'
    }
   }
  stages {
    stage('Test') {

      steps {
     container('maven') {
       echo "${cucumberTag}"
        sh "mvn test"
        }
      }
    }
  }
}
